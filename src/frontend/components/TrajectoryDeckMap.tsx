"use client";

/**
 * Placeholder component reserved for the bespoke deck.gl integration the
 * frontend team plans to build. Replace the body with the actual map when ready.
 */
export function TrajectoryDeckMap() {
  return null;
}
